package com.skiletro.wheelwitch.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.unit.dp

/**
 * Shared corner radius used by primary action buttons throughout the app.
 */
val buttonShape = RoundedCornerShape(14.dp)

/**
 * Shared corner radius used by large content sections (onboarding step
 * cards, license cards).
 */
val sectionShape = RoundedCornerShape(20.dp)

/**
 * Shared corner radius used by medium cards and panels (hub options,
 * health status, stat cards).
 */
val surfaceShape = RoundedCornerShape(16.dp)

/**
 * Shared corner radius used by list rows and small interactive items
 * (room list, leaderboard rows, time-trial tracks, health checks).
 */
val chipShape = RoundedCornerShape(8.dp)
